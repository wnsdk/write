spring:
  datasource:
    url: jdbc:mysql://write-db:3306/write_db
    username: write
    password: write
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create  # or create, create-drop, validate based on your needs
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        
test: prod

jasypt:
  encryptor:
    password: ${JASYPT_ENCRYPTOR_PASSWORD}
