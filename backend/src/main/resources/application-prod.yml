spring:
  datasource:
    url: jdbc:mysql://3.37.232.227:3306/write_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create  # or create, create-drop, validate based on your needs
      dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        
test: prod

jasypt:
  encryptor:
    password: ${JASYPT_ENCRYPTOR_PASSWORD}
