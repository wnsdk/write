spring:
  datasource:
    url: jdbc:mysql://mysql:3306/write
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create  # or create, create-drop, validate based on your needs
      dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jasypt:
  encryptor:
    password: ${JASYPT_ENCRYPTOR_PASSWORD}
